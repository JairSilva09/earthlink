import { Convert12HourFormatPipe } from './convert12-hour-format.pipe';

describe('Convert12HourFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new Convert12HourFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
